# ABOUTME: Docker Compose configuration for HotStock
# ABOUTME: Single command deployment with persistent SQLite storage

services:
  app:
    build: .
    ports:
      - "8787:3000"
    volumes:
      - stock-data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/stock-picker.db
    restart: unless-stopped

volumes:
  stock-data:
